ScreenFullPicture:
    name: "screen full picture"

    FitImage:
        id: _full_image

    MDIconButton:
        icon: "arrow-left"
        md_bg_color: 1, 1, 1, .6
        x: 12
        pos_hint: {"top": .99}
        on_release:
            if not app.A5_edition : app.manager.restore_screen() 
            app.manager.current = "screen A5"